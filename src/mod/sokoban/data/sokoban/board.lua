data:add_type {
   name = "board",
   fields = {
      {
         name = "board",
         type = types.string,
         template = true
      }
   }
}

require("mod.sokoban.data.sokoban.board.builtin")
