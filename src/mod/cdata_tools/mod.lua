return {
   id = "cdata_tools",
   description = [[
A set of tools for parsing and converting custom data from various variants.
]],
   version = "0.1.0",
   dependencies = {
      sjis = ">= 0",
      extlibs = ">= 0",
      elona = ">= 0"
   }
}
